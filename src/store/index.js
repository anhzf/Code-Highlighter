import Vue from 'vue';
import Vuex from 'vuex';
// modules
import highlighter from './highlighter';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        highlighter,
    },
});
